extends index.pug

block content

  h1 Category Name

  each product in products
      ul
        h3
          a(href=`/products/${product.id}`) #{product.name} 
        p Qauntity: #{product.quantity}
        p $#{product.price}